// S-WIZARD-NOMINA-V3 · sub-tarea 2 · placeholder.
// El contenido real lo escribe la sub-tarea 3 (pantalla única ATLAS v8).
// Este componente sustituye al antiguo `NominaWizard` (3 pasos · 1088 líneas)
// y mantiene la ruta `/gestion/personal/nueva-nomina` operativa entre commits.

import React from 'react';

const NominaPage: React.FC = () => (
  <div
    style={{
      padding: 32,
      fontFamily: 'Inter, system-ui, sans-serif',
      color: 'var(--navy-900)',
    }}
  >
    Cargando…
  </div>
);

export default NominaPage;
