# DEUDA · Design System · lo que queda tras fijar la fuente única (2026-09-11)

> Origen: `docs/AUDITORIA-DESIGN-SYSTEM-fuente-unica-2026-09-11.md` (§5, §7, §8). Decisión de Jose: **no bloquea**, se anota y se ataca en tareas aparte. Fuente de verdad única: `src/design-system/v5/tokens.css` + `docs/GUIA-DISENO-V5-atlas.md`.

| # | Deuda | Dónde | Qué habría que hacer |
|---|---|---|---|
| 1 | **Dos tipografías UI cargadas**: Inter (V5, `tokens.css` `--atlas-v5-font-ui`, 244 usos) e IBM Plex Sans (`index.css` `--font-base` en `html, body`, Tailwind `sans`, 25 usos). La guía §1 regla 3 dice Plex; el código real usa Inter | `src/index.tsx:1-2`, `src/index.css`, `tailwind.config.js`, guía §1/§2.3 | Decidir UNA (recomendación: Inter, la de las pantallas reales y de los mockups conformes), corregir la guía, quitar la carga de `@fontsource/ibm-plex-*` y repuntar `--font-base` |
| 2 | **`toastService.tsx` usa variables CSS que no existen** (`--hz-success`, `--hz-warning`, `--hz-error`, `--hz-info`, `--brand-navy`, `--brand-teal`: solo son nombres Tailwind) → esos toasts salen sin color de borde/icono | `src/services/toastService.tsx:20-25` | Apuntar a `--atlas-v5-*` (brand · pos · warn · neg) |
| 3 | **Guía §2.1 desfasada respecto a `tokens.css`**: faltan en la guía `gold-wash-2`, `gold-light`, `pos-bright`, `pos-border`, `pos-dark`, `warn-border`, `cripto*`, `room-*`, `equity-*`, `cartera-*`, `chart-*`, `punto-*`, `bank-*`, `on-brand-*`, `chart-accent-wash`, `focus-ring-color`. `tokens.css` cita secciones inexistentes ("§Z.2.1", "§10 punto de cuenta", "§5.1 charts"); `index.css:642` cita "§B.4" | `docs/GUIA-DISENO-V5-atlas.md` §2.1 · `tokens.css:94,268,299` | Regenerar §2.1 desde `tokens.css` (manda el código) y corregir las citas |
| 4 | **Duplicados dentro de `tokens.css`**: `on-navy-1` = `white`; `navy-text-2`/`navy-text-disabled` solapan `on-navy-4`/`on-navy-7`; lista de bancos repetida (`brand-*` y `bank-*`) | `tokens.css:57-59,66-67,102-109,362-373` | Consolidar (A.2 pendiente en el propio fichero) |
| 5 | **Hex fuera de `tokens.css` en pantallas vivas**: `PanelAnalitico.module.css:136-140` (rampa de mapa de calor, 5 sin token + `#F3EAD6` = `gold-wash`), `inversiones/helpers.ts:17-19` (paleta chart v4), `InversionesGaleria.module.css` (logos MyInvestor/BBVA/BNP; `#004481` ya es `--atlas-v5-brand-bbva`) y `.cartaLogo.smartflip` usando `--atlas-v5-pos` como color de marca | ver auditoría §5 | Crear tokens (rampa calor, marcas) y repuntar |
| 6 | **Hex en pantallas legacy** (no V5): `financiacion/helpers.ts` (22), `horizon/proyeccion/.../ProjectionChart.tsx` (8), `utils/accountHelpers.ts` (6), `horizon/analisis-cartera/AnalisisCartera.tsx` (4), `horizon/fiscalidad/historico/ImportarDeclaracionWizard.tsx` (2), páginas `pages/dev/*` (38) | auditoría §5 | Caen cuando se migre o borre cada pantalla; el indicador 7 de `scripts/health.mjs` (trinquete) los vigila |
| 7 | **Capa de compatibilidad v4** (`src/index.css`, `tailwind.config.js`): 53 ficheros usan alias `--navy-*`/`--grey-*`/`--hz-*`/`--atlas-blue`… y 36 usan utilidades Tailwind de color. Ya apuntan a V5, pero son nombres viejos | auditoría §2 filas C-D · §6.3 | Migrar pantalla a pantalla a `--atlas-v5-*`; cuando queden 0 consumidores, borrar la capa |
| 8 | **Dos conciliaciones**: `src/modules/horizon/conciliacion/v2/` (con `conciliacion-v2.css`, 971 líneas, conforme a V5) sigue viva vía `GestionInmuebles` mientras la conciliación real vive en `tesoreria/v6/conciliar/` | auditoría §2 fila K | Deuda de módulo, no de estilo |
| 9 | **`components/common/EmptyState.css` duplica `design-system/v5/EmptyState`**; `mi-plan/pages/PresupuestoAnual.css` es CSS global (no módulo) | auditoría §2 filas L-M | Unificar en el DS · convertir a `.module.css` cuando se toque |
| 10 | **8 mockups HTML con tokens fuera de paleta** (`--gold #c8a04a`, `--bg #F8F6F1`, `--ink #0E1423`…), entre ellos `docs/audit-inputs/atlas-tesoreria-v8.html` (origen del verde/rojo) | auditoría §8.1 | Marcar como superados o corregir su `:root` con los valores de `tokens.css` |
| 11 | **Usos de `MoneyValue` con `tone="auto"` explícito** (7: mi-plan 3, fiscal 2, personal 2): revisar uno a uno que sean deltas de rentabilidad y no saldos | `grep -rn 'tone="auto"' src` | Cambiar a `ink` los que sean importes |
| 12 | **Estilos de wrapper ahora inertes**: `PatrimonioResumenInmueble.module.css` `.cardValue.neg` (deuda pendiente) ya no colorea la cifra porque el importe va en tinta por regla | `PatrimonioResumenInmueble.tsx:224` | Quitar la clase cuando se toque el componente |
