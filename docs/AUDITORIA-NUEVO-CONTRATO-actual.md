# AUDITORÍA · Flujo "Nuevo contrato" · foto actual (2026-08-19)

> Informe de solo lectura leído del código vivo, base para el rediseño de la creación de contratos de alquiler. Documenta **lo que existe y funciona** (para reutilizarlo) y lo que falta respecto al modelo objetivo (RC = átomo físico · unidad de alquiler = agrupación 1..N RC definida por el contrato · dos ejes régimen × gestión · garantizada con bruto diferido · catálogo de unidades alquilables).
>
> Toda afirmación lleva evidencia `archivo:línea`. Lo inexistente se declara con los greps hechos.

## Preflight

- **Rama y commit:** `claude/new-session-hgfunj` sobre `d3daaae` («feat(tesoreria): rediseño V9…», main), árbol limpio. **`DB_VERSION = 89`** (`src/services/db.ts:56`).
- **Las dos rutas de las capturas, localizadas:**
  - **Selector inicial** «Alquiler directo · Gestión delegada» = `src/modules/inmuebles/wizards/NuevoContrato.tsx` («Paso 0»), montado por la ruta **`/contratos/nuevo`** (`App.tsx:1317-1321`, lazy import `App.tsx:105`).
  - **Wizard de 5 pasos** con resumen en vivo = `src/modules/inmuebles/wizards/NuevoContratoWizard.tsx` (rama `directo` del selector).
  - **Formulario «Datos del contrato de gestión»** = `src/modules/inmuebles/wizards/NuevoContratoGestionWizard.tsx` (rama `gestion` del selector). Título literal en `NuevoContratoGestionWizard.tsx:127`.
  - **Cómo se enlazan:** `NuevoContrato.tsx:13,22` guarda `tipo: 'directo' | 'gestion' | null` en estado local y bifurca en `:24-25`: `directo` → `<NuevoContratoWizard/>`, `gestion` → `<NuevoContratoGestionWizard onBack/>`. **Ambos escriben el mismo tipo `Contract` en el mismo store `contracts` vía el mismo `saveContract`** (`contractService.ts:104-131`; llamadas en `NuevoContratoWizard.tsx:173` y `NuevoContratoGestionWizard.tsx:88`).
  - Ruta hermana: `/contratos/gestion/anexar` → `AnexarSubcontratoForm` (`App.tsx:1324-1328`) para anexar subcontratos de inquilino a un padre de gestión.

---

## 1 · Arquitectura del flujo

**Rutas y puertas** (todas en `src/App.tsx`): `/contratos` monta el shell `InmueblesPage` (`:1302-1306`) — importa porque los wizards leen `properties` del outlet context (`NuevoContratoWizard.tsx:31`, `InmueblesContext.ts:4`). `/contratos/nuevo` → `NuevoContrato` (`:1317-1321`). Alias `/alquileres/*` → `/contratos/*` (`:1334`, lógica en `components/routing/resolveAlquileresRedirect.ts:6`). Entradas: botón «Nuevo contrato» y CTAs de `ContratosListPage.tsx:223,238,282`, con prefill `?inmueble=<id>` desde Disponibilidad (`:322-326`); onboarding `?from=empezar` (`onboarding/empezar/bloques/ContratosBloque.tsx:76`). **Edición reutiliza la misma ruta** `?edit=<id>` (`ContratosListPage.tsx:134-141`; subcontratos van a `gestion/anexar?edit=`).

**El selector (Paso 0):** dos cards (`NuevoContrato.tsx:40-48` directo, `:50-58` gestión). **La elección NO se persiste como campo**: el modo delegado queda implícito en la presencia del bloque embebido `Contract.gestion` (`types-contratos.ts:314-320`; escrito por `gestionGarantizadaPayload.ts:140-148`, nunca por el camino LAU). Los consumidores detectan por `gestion != null` / `gestionPadreId != null` (`ContratosListPage.tsx:136-140`; predicados en `modules/inmuebles/utils/gestionDelegada.ts:16-23`).

**El wizard:** un solo componente con TODO el estado local (sin reducer ni contexto): `step: StepKey` (`NuevoContratoWizard.tsx:51`, tipo `:27`), `form: FormState` (`:52-55`; interfaz de 15 campos en `contratoWizardHelpers.ts:6-23`, defaults `:38-54`). Pasos (`:100-106`): `donde` (:284-368) · `inquilino` (:370-426) · `economico` (:428-494) · `documentos` (:496-575) · `firma` (:577-671). Navegación: `canAdvance` por paso (`:108-130`; documentos y firma devuelven `true` fijo), `handleNext` (`:229-239`), stepper solo permite saltos hacia atrás (`:274-278`).

**Persistencia al final** (`handleCrearContrato`, `:135-199`): valida vía `construirPayloadCompleto` (`contratoWizardPayload.ts:29-76`) → `saveContract(payload)` (`:173`) → verificación de relectura `getContract(id)` (`:177-180`) → `vincularAccesorioDesdeContrato` (`:181`, escribe el store `vinculosAccesorio`, no-fatal) → navega. `saveContract` (`contractService.ts:104-131`) rellena defaults, **sintetiza** `documentoContrato` (`:68-80`) y `firma` (`:82-102`) y hace un único `db.add('contracts', …)` (`:129`). **Sin efectos en tesorería ni en `documents` al crear** (ver §5 y §8). Borrador: `construirPayloadBorrador` → `estadoContrato: 'sin_firmar'` (`contratoWizardPayload.ts:83-111`). Edición: `updateContract` con whitelist explícita de 13 campos que preserva firma/estado/cuenta (`NuevoContratoWizard.tsx:151-164`, comentario `:148-150`).

**Store:** `contracts` keyPath `id` autoIncrement con UN solo índice, el **legacy** `propertyId` (`upgrade-a.ts:47-50`; `db.ts:94`) — que los wizards modernos nunca escriben (solo escriben `inmuebleId`), así que `getContractsByProperty` hace `getAll` + filtro (`contractService.ts:194-198`).

**Tipo `Contract`:** `src/services/db/types-contratos.ts:158-379`. Campos clave: `inmuebleId` :162 · `unidadTipo: 'vivienda'|'habitacion'` :163 · `habitacionId?` :164 · `modalidad: 'habitual'|'temporada'|'vacacional'` :167 · `inquilino{…, cotitulares?}` :170-184 · `rentaMensual` :191 · `diaPago` :192 · `indexacion` :196 + `indexOtros?` :197-201 + `historicoIndexaciones` :204-209 · `historicoRentas?` :219 · fianza :222-228 · `cuentaCobroId` :231 · `estadoContrato` :236 · `documentoFirmado?` :253 · `documentoContrato?` :256-261 · `firma?` :264-273 · **`reduccion?`** :276-280 + flags fiscales :286-290 · **`gestion?: GestionDelegada`** :320 · **`gestionPadreId?`** :327 · legacy :330-371 · `documents: number[]` :374.

**Anomalías de arquitectura detectadas:** (a) con `?edit=`, el Paso 0 se salta inicializando `tipo='directo'` (`NuevoContrato.tsx:22`) → **un contrato de gestión editado por esta ruta abre en el wizard LAU** y su `updateContract` pisaría el `inquilino`-agencia (`gestionGarantizadaPayload.ts:126`) conservando `gestion` (spread en `contractService.ts:155`); (b) el prefetch de ruta precarga `NuevoContratoWizard` cuando la ruta monta `NuevoContrato` (`navigationPerformanceService.ts:116-118` vs `App.tsx:105`).

---

## 2 · Paso 1 · «Dónde» (unidades)

**Selector de INMUEBLE — directo a `properties`, sin catálogo.** Lista = `properties` del outlet (`NuevoContratoWizard.tsx:31`), cargadas en `InmueblesPage.tsx:22-26` con único filtro `state === 'activo'`; el `<select>` (`:295-308`) **no filtra** por `modoExplotacion`, `tipoActivo` ni ocupación (un parking aparece como principal contratable; un piso 100 % ocupado sigue saliendo). Prefill `?inmueble=` (`:35-40,54`).

**Catálogo de «unidades alquilables» — NO EXISTE como entidad.** Greps: `unidadAlquilable` → **0 hits**; `catalogo.*unidad` → 0 («catálogo» solo en `catalogoModalidadInmueble.ts` = gastos sugeridos, `wizards/utils/catalogoModalidadInmueble.ts:94,115`); `unidadesArrendables` → solo el campo numérico `Property.explotacion.unidadesArrendables?` (`types-inmuebles.ts:146`) y consumidores KPI/import (`contractDraftService.ts:631`, `mapaTemporalService.ts:56`, `PasoInmuebles.tsx:45-75`…). Cada consumidor recuenta unidades con su propia fórmula y **el wizard no usa ninguna** (grep `unidadesArrendables|modoExplotacion|explotacion` en los dos wizards → 0 hits).

**HABITACIÓN — sintética desde `bedrooms`.** El selector solo aparece si `habitacionesTotales > 1` (`:316`), y esa cuenta es **exclusivamente `Property.bedrooms`** (`:98`); las opciones se generan `hab-1..hab-N` (`:325-329`), opción vacía «— inmueble completo —» (`:324`). El wizard **ignora** `modoExplotacion` y `alquilerPorHabitaciones.numeroHabitaciones` — el importador sí los usa con cascada de fallbacks y mínimo 2 (`contractDraftService.ts:628-638`): un piso `por_habitaciones` con `bedrooms ≤ 1` no muestra selector en el wizard aunque el importador sí lo pediría. El contrato guarda `unidadTipo` derivado (`contratoWizardPayload.ts:25-26`) + `habitacionId` string (`:56-57`); `modoExplotacion` no se denormaliza (`docs/services/alquileres-modelo.md:124-126`). **Bug de UI:** cambiar de inmueble no resetea `habitacionId` (a diferencia de `AnexarSubcontratoForm.tsx:133-135`) → se puede guardar `unidadTipo:'habitacion'` sobre un inmueble sin habitaciones.

**ACCESORIO «que se alquila junto» — 1 solo, inmueble↔inmueble, fuera del Contract.**
- Componente `CampoAccesorioContrato.tsx` (51 líneas): `<select>` simple, `value: number | null` (`:12-15`; estado `NuevoContratoWizard.tsx:58`) — **no es array**; elegir un segundo sustituye al primero.
- Elegible: **cualquier inmueble activo** salvo el principal (`CampoAccesorioContrato.tsx:34-40`); sin filtro por `tipoActivo`. (`getCandidatosAccesorio` del servicio existe pero solo lo usa su test — `vinculoAccesorioService.ts:97-109`, `__tests__/vinculoAccesorioManual.test.ts:7,66`.)
- Copy exacta: label «Accesorio que se alquila junto (opcional)» (`:27`), help «Parking o trastero con referencia catastral propia que se alquila con este piso. Su amortización y gastos se suman al principal mientras dure el alquiler.» (`:42-45`).
- **Dónde se guarda la vinculación:** NO en el Contract (grep `accesorio` en `types-contratos.ts` → 0 hits; `accesorioId` no entra en ningún payload). Va al store **`vinculosAccesorio`** (`upgrade-a.ts:441-448`, índice único `[principal, accesorio, ejercicio]`; tipo `types-fiscal.ts:546-557`: `inmueblePrincipalId · inmuebleAccesorioId · ejercicio · fechaInicio/fin · estado · origenCreacion`), escrito tras el save por `vincularAccesorioDesdeContrato` (`NuevoContratoWizard.tsx:181-182` → `vinculoAccesorioService.ts:77-95`, idempotente `:29-69`, errores tragados `:91-94`).
- **¿Llega al caso «Hab 4 + Finca 5»? NO.** El vínculo es entre **inmuebles**, sin `habitacionId` (`types-fiscal.ts:546-557`), 1 principal + 1 accesorio; no cabe una tercera unidad ni la relación con una habitación concreta. Semántica documentada: parking/trastero con RC propia pegado a un piso (`vinculoAccesorioService.ts:4-8`).
- **Huecos del camino de escritura:** en edición no se prefilla ni se escribe (estado arranca `null` `:58`; `contratoAForm` no lo recupera `contratoWizardHelpers.ts:57-73`; la rama edit retorna en `:170` antes de `:181`); en borrador tampoco (`:205-227`); nadie pone `estado:'inactivo'` al terminar el contrato.
- **Y el motor IRPF no lo lee:** grep `vinculosAccesorio` en `irpfCalculationService.ts` → **0 hits**; fiscal enlaza accesorios por `fiscalData.isAccessory` + `mainPropertyId` (`irpfCalculationService.ts:643-665`, consumido `:730,:905-907`). El vínculo que escribe el wizard no es el que lee el cálculo fiscal.

**MODALIDAD** — exactamente 3 opciones cerradas: «Habitual · LAU 5 años» / «Temporada» / «Vacacional» (`NuevoContratoWizard.tsx:340-342`; tipo `contratoWizardHelpers.ts:9` = `types-contratos.ts:167`). **No existe campo `regimen`** (grep en `types-contratos.ts` → 0). Fechas: `fechaInicio` = hoy (`contratoWizardHelpers.ts:42`), editable; `fechaFin` arranca vacía y el «+5 años LAU» **no se aplica en la UI** (ningún efecto lo calcula; el help `:362-364` lo promete y el resumen muestra «—» `:752-757`) — se aplica solo al construir el payload (`contratoWizardPayload.ts:42-45` → `calculateHabitualEndDate`, `contractService.ts:27-31`); no-habitual sin fin → error `:45`. Siempre editable (`:354-361`), validación fin > inicio (`:46-47`).

**Multi-unidad:** estrictamente **1 inmueble + ≤1 habitación + ≤1 accesorio** (selects simples `:295-308`, `:319-330`, `CampoAccesorioContrato.tsx:28`; escalares en el tipo `:162-164`). Lo único plural es `cotitulares` (personas, no unidades, `types-contratos.ts:176-183`). Agrupar N unidades hoy = N contratos.

---

## 3 · Paso 2 · Inquilino

**Campos** (`NuevoContratoWizard.tsx:370-426`): Nombre (:379-387) · Apellidos (:388-396) · NIF/NIE (:397-405, solo `.toUpperCase()` :403) · Teléfono (`type="tel"` :406-414) · Email (`type="email"` :415-423). Nada más (sin fecha de nacimiento, domicilio, avalista, segundo inquilino ni flag «inquilino joven»).

**Validaciones:** para avanzar solo nombre+apellidos+NIF no vacíos (`:113-117`); teléfono y email se vuelven obligatorios recién en «Crear contrato» (`contratoWizardPayload.ts:38-41`) — puedes pasar el paso 2 y bloquearte en el 5. **Sin validación de formato/letra de NIF/NIE en todo el flujo** (el único regex NIF del repo está en `horizon/conciliacion/v2/components/AddMovementModal.tsx:46-51`, sin relación). `validateContract` (`contractService.ts:482-576`) sí validaría, pero es **código muerto** (su única aparición es la definición).

**Persistencia:** embebido en `Contract.inquilino` (`types-contratos.ts:169-184`; construido en `contratoWizardPayload.ts:17-23`). **No existe store `inquilinos`** (esquema completo `db.ts:90-355` sin store de inquilinos; grep `inquilinos` en services → solo el contador `inquilinosNuevos` de `contractImportCreationService.ts:19,30,42`). **No hay reutilización de inquilino existente** (sin picker ni dedupe por DNI; la identidad/dedupe existe solo en el import: `inquilinoExistenteId`, `import/__tests__/PasoRevision.test.tsx:22,60`). **`cotitulares[]` (V78) no se captura en el wizard** (`inquilinoDe` omite la clave, `contratoWizardPayload.ts:17-23`); solo lo escriben XML AEAT/import (`declaracionOnboardingService.ts:1016,1070-1077`, `contractDraftService.ts:485-556`, `atlasTemplateParserService.ts:82,196`) y el backfill (`post-open.ts:72-93`).

---

## 4 · Paso 3 · Económico

**Campos** (`NuevoContratoWizard.tsx:428-494`): Renta mensual (:437-448 → `rentaMensual`, `contratoWizardPayload.ts:31,62`) · Día de pago 1-31 (:449-460 → `diaPago` :32,63) · Fianza en **mensualidades** 0-6 (:461-473 → `fianzaMeses` + `fianzaImporte = round(renta×meses)` :33,67-68; el importe NO es editable pese al comentario del tipo `types-contratos.ts:223`) · Indexación `none|ipc|irav|otros` (:474-486 → :65) · Cuenta de cobro (`CuentaCobroField.tsx:16-47` → :70). Defaults: día 1, 2 mensualidades, `ipc` (`contratoWizardHelpers.ts:49-53`; el wizard NO llama a `suggestIndexation`, `contractService.ts:19-24`). Hardcoded en payload: `margenGraciaDias: 5`, `fianzaEstado:'retenida'`, `estadoContrato:'activo'`, `documentoFirmado: true` (`contratoWizardPayload.ts:64-72`). Sin forma de pago, sin garantías adicionales operativas (`additionalGuarantees` legacy con 0 lectores/escritores), sin `fechasFianza`, sin seed de `historicoRentas`. En edición **se omite `cuentaCobroId` a propósito** (`:148-150,151-164`): cambiar la cuenta en modo edición no hace nada.

**Reducción IRPF — NO se captura ni calcula aquí.** El wizard no escribe `reduccion` ni `zonaTensionada`/`inquilinoJoven`/`rebajaRenta5pct`/`rehabilitacion`/`fechaFirmaContrato` (grep `reduccion` en `wizards/` → 0 hits; payloads `contratoWizardPayload.ts:52-111`). Los campos existen en el modelo (`types-contratos.ts:276-290`) pero:
- **Único escritor de `Contract.reduccion` en todo el código: el importador CSV** (`contractImportCreationService.ts:137-140`, columna «reducción IRPF» del template, `atlasTemplateParserService.ts:34,81,195`). Los flags de zona tensionada/joven/rebaja/rehabilitación **no tienen escritor ninguno** → las ramas 70 % y 90 % son inalcanzables en la práctica.
- Motor fiscal A — `irpfCalculationService.calcularPorcentajeReduccionContrato` (`:322-367`): temporada/vacacional → 0; override `reduccion` → su %; si no, por fecha (`fechaFirmaContrato ?? firma.fechaFirma ?? fechaInicio`): < 26/05/2023 → 60 %, después → 90/70/60 según flags (nunca puestos) o **50 %** general (`:361-366`). Todo contrato habitual creado a mano cae en la rama por fecha: 60 o 50.
- Motor fiscal B — `fiscalSummaryService.detectarPorcentajeReduccion` (`:585-595`) lee `c.reduccionLeyVivienda`, **campo fantasma que no existe en `Contract` y nadie escribe** (grep: solo esas 2 líneas) → siempre devuelve el hardcoded **60 %** (`:595`) que alimenta la casilla 0150 (`:762-763`). **Dos motores divergentes y ninguno alimentado por el wizard.**

**Renta — cifra global única.** `rentaMensual: number` escalar (`types-contratos.ts:191`); greps `rentas?:|desglose?:|porUnidad?:` en el tipo → **0 hits**; los repo-wide de `desglose` son de presupuesto/fiscal. `historicoRentas` es serie temporal de la misma cifra, no desglose (`:29-43,219`); la renta no se divide entre cotitulares (`:180`). **Por habitaciones = un Contract por habitación** (4 habitaciones = 4 pasadas del wizard, 4 filas hermanas con el mismo `inmuebleId`); en delegada, N hijos con `gestionPadreId` sumados en `facturacionGestionService.ts:180-183`. Legacy `scope`/`selectedUnits` (`:331-332`) sin uso.

**Indexación:** solo el tipo; sin porcentaje, mes de revisión ni fórmula. **Bug:** el wizard ofrece `otros` (`:484`) pero nunca escribe `indexOtros` (la delegada sí lo hace bien: `NuevoContratoGestionWizard.tsx:339-353` + `gestionGarantizadaPayload.ts:152-154`). **Nadie aplica índices después:** `historicoIndexaciones` solo se inicializa a `[]` (grep fuera de tests: `contractService.ts:117` y los 4 payloads); ningún código añade entradas ni sube `rentaMensual`. El único consumidor del campo es la proyección, que ignora el índice concreto: `rentasContratosEngine.ts:42-48` — `none` → 0 %, todo lo demás → el global `supuestos.subidaRentasPct` (documentado `:6-12`). `ipc`/`irav`/`otros` son hoy conductualmente idénticos.

---

## 5 · Pasos 4 y 5 · Documentos y Plantilla/firma

**Los dos pasos son decorativos: no persisten nada.**

**Documentos** (`NuevoContratoWizard.tsx:496-575`): 4 tiles hardcoded (`:514-519`: DNI·NIE inquilino / Contrato firmado / Justificantes de ingresos / Aval), cada uno un botón cuyo único onClick es un toast «Subir … · sub-tarea follow-up · enlace con bandeja Inbox + tipado documento.» (`:520-527`); hints con el literal «· pendiente» (`:553-555`). **No hay `<input type="file">` en todo `src/modules/inmuebles/`** salvo los 3 importadores XLSX (grep verificado). `saveDocumentWithBlob` (`db/documents.ts:56`) tiene 3 call sites y ninguno es el wizard; `metadata.entityType:'contract'` existe en el tipo (`types-inmuebles.ts:632`) pero **nadie lo escribe nunca** (escritores solo `'property'` y `'personal'`). El payload lleva `documents: []` fijo (`contratoWizardPayload.ts:73,109`) y `FormState` no tiene campo de documento. El callout «puedes adjuntar más tarde desde la ficha» (`:559-573`) apunta a otra promesa: el «Añadir» de la ficha también es toast «próximamente · T3.7» (`DrawerFichaContrato.tsx:582-596`).

**Plantilla y firma** (`:577-671`): 3 cards de plantilla hardcoded (`:597-612`) cuyo click solo lanza el toast «selección registrada (follow-up persistencia)» (`:616-619`) — **falso**: no hay estado de selección y `normaliseDocumentMetadata` **deriva** la plantilla de `unidadTipo`/`modalidad` descartando el click (`contractService.ts:68-80`). Sin generación de PDF ni e-firma: grep `signaturit|docusign|adobesign|factorid` → 3 hits (el union del tipo `types-contratos.ts:266`, un literal default `contractService.ts:94` y el propio copy «llegan en sub-tarea follow-up» `:654-669`); jsPDF/pdf-lib solo se usan en informes. **Estado de firma que sí se escribe:** `documentoFirmado: true` **hardcoded** en todo contrato completado (`contratoWizardPayload.ts:72` — sin que el usuario haya subido ni firmado nada; borrador `false` `:108`), y `firma` sintetizada a `{metodo:'manual', estado:'borrador', emails:[]}` (`contractService.ts:82-102`). **API de firma muerta** (0 call sites): `updateSignatureStatus` :327-350, `sendContractForSignature` :352-373 (solo cambia estado local a `'enviado'`, nada sale del navegador), `markContractAsSigned` :375-379; `validateContract` :482-576 con gates reales de plantilla/firma que el wizard esquiva.

**Submit final** (orden, `:135-199`): guard re-entrada → validar payload (resultado, no throw; el botón queda vivo si falla) → `saveContract` → **verificación de relectura** (`:177-180`, detalle bueno a conservar) → vincular accesorio (no-fatal) → toast → navigate (`/empezar/contratos?done=contrato` o `/contratos?tab=activos` `:189`; edición y borrador van a `?tab=vigentes` `:169,:217`). **Sin generación de eventos al guardar** (ver §8-nota).

**Tests:** `__tests__/NuevoContratoWizard.test.tsx`, `NuevoContratoWizard.fecha.test.tsx`, `NuevoContrato.test.tsx`, `NuevoContratoGestionWizard.test.tsx`, `AnexarSubcontratoForm.test.tsx`, `anexarSubcontratoPayload.test.ts`, `gestionGarantizadaPayload.test.ts` (+ `backfillDocumentoFirmado.test.ts`). Hueco: los pasos 4-5 solo se atraviesan, nunca se asertan; no existe `contratoWizardPayload.test.ts` (el `documentoFirmado: true` hardcoded está sin test directo).

---

## 6 · Ruta «Gestión delegada · Datos del contrato de gestión»

**Formulario aparte de una sola página** (sin stepper; botón único `NuevoContratoGestionWizard.tsx:382-386`), que solo reutiliza el CSS del wizard (`:20`, cabecera `:1-5`). Aside resumen propio (Comisión/Liquidación/Fianza/Indexación/Plazo/Agencia, `:390-420`) — **no** el «Resumen en vivo» del wizard.

**Campos reales** (`NuevoContratoGestionWizard.tsx`): Inmueble :132-146 (prefill `?inmueble=` :44-48) · Agencia :150-156 · NIF/CIF :159-165 · **«Comisión de la agencia»** :169-178 con 3 tipos: **`garantizada` = «Renta garantizada»** (:175), `porcentaje` (:176), `fees` (:177) — la garantizada es un TIPO de comisión, no un concepto aparte · **«Liquidación»** :181-189: `agencia_neto` = «La agencia cobra · te ingresa el neto» (:187) / `propietario_bruto` = «Tú cobras · pagas a la agencia» (:188) · **Renta garantizada €/mes** :192-208 (solo si tipo garantizada) · % comisión :209-226 · fees por habitación/fijo :229-252 · captación :256-287 · **Día de cobro** :291-301 · **Fianza €** opcional :302-314 (sin `fianzaMeses`) · **Inicio/Fin** :316-333 (fin obligatorio, sin +5 LAU) · Indexación `ipc|otros|none` :335-346 (**sin IRAV**, `gestionGarantizadaPayload.ts:16`) + fórmula si `otros` :347-356 · Cuenta de cobro :361-367 (mismo `CuentaCobroField`). NO tiene: datos de inquilino, modalidad, unidad/habitación, documentos, plantilla/firma, reducción, accesorio.

**Modelo de datos:** el discriminador es la **presencia** de `Contract.gestion: GestionDelegada` (`types-contratos.ts:320`; hijo = `gestionPadreId` :327; sin boolean/enum de gestión). `GestionDelegada` (`:121-155`): `agenciaNif` :123 · `modeloIngreso: 'garantizada'|'traspaso'` :124 · `rentaGarantizada?` :130 (espejada en `rentaMensual`, doc :126-129) · `honorarios: HonorarioAgencia[]` :132 (tipo :102-112) · `liquidacion?: 'agencia_neto'|'propietario_bruto'` :144 · `comisionTipo?` :152 · `comisionPorcentaje?` :154. Payload (`gestionGarantizadaPayload.ts:122-154`): `inquilino` = **la agencia** (:126), `modalidad:'habitual'` y `unidadTipo:'vivienda'` **hardcoded** (:124-125), `fianzaMeses: 0` (:135), `rentaMensual` = garantizada o **0** en %/fees (:53-55,:64,:129). Efecto extra: alta de la agencia en `proveedores` con `tipos:['gestion']` (`NuevoContratoGestionWizard.tsx:87` → `agenciaGestionService.ts:13-33`).

**Neto vs bruto — CENTRAL: solo se guarda el neto.** Greps `rentaBruta|brutoFiscal|comisionAgencia|ingresoBruto` → 0 hits relacionados con contratos. El **bruto no se almacena: se deriva de los subcontratos anexados a mano** (`resumenFacturacion`, `facturacionGestionService.ts:172-201`: facturación = Σ hijos; comisión garantizada = `max(0, facturación − garantizado)` :66; neto = facturación − comisión :199). Fiscal: `irpfCalculationService.ts:793` **excluye explícitamente al padre** de los ingresos íntegros (comentario :789-792) y suma la comisión como gasto (casilla 0112, `:893-902` vía `resumenFacturacion`). **Consecuencia:** una garantizada sin subcontratos anexados → `ingresosIntegros = 0` y `comisionGestion = 0`: el ingreso garantizado **desaparece del IRPF**; no hay fallback a `rentaGarantizada` como ingreso fiscal ni cálculo de bruto desde neto en ningún sitio.

**¿Mismo wizard/tipo/eventos?** Formulario aparte, mismo tipo `Contract`, mismo store, mismo `saveContract` (`NuevoContratoGestionWizard.tsx:11,88`). Los eventos de tesorería salen del mismo generador (§8-nota) con una capa consciente de la delegada: `planificarGestionMes` (`gestionTesoreria.ts:90-153`, llamada `treasurySyncService.ts:298-307`) — `agencia_neto`: hijos suprimidos (:124) y el padre emite el neto (:127-129); `propietario_bruto`: padre suprimido (:133), hijos emiten heredando la cuenta del padre (:135-137) + apunte de comisión (`sourceType:'comision_gestion'`, `treasurySyncService.ts:373-390`).

**`PanelGestionDelegada.tsx`** (montado en la ficha solo si hay `gestion`, `DrawerFichaContrato.tsx:465-469`): agencia :63-65, renta garantizada :66-69, fianza :70-75, bloque Facturación con selector de ejercicio :79-97, filas Subcontratos anexados / Facturación Σ / Comisión estimada (+captación) / **Neto para ti** :98-119, lista de hijos :121-129, CTA «Anexar contrato de inquilino» → `/contratos/gestion/anexar?padre=` :56,:137-140. **No muestra liquidaciones reales** (sin registros mensuales ni conciliación contra Tesorería; la comisión es estimación por fórmula, deliberado por `irpfCalculationService.ts:888-892`).

**Edición delegada — hueco:** `?edit=` salta el Paso 0 a `'directo'` (`NuevoContrato.tsx:22`), así que un contrato de gestión no puede re-editarse por su formulario (solo los subcontratos van a `gestion/anexar?edit=`, `ContratosListPage.tsx:136-141`).

---

## 7 · Resumen en vivo

**JSX inline dentro del propio wizard, no un componente**: `<aside>` en `NuevoContratoWizard.tsx:732-792` (grep «Resumen en vivo» en la delegada → 0). Columna derecha sticky de un grid `1fr 360px` que colapsa bajo 1100px (`NuevoContratoWizard.module.css:1-9,101-109`). **Se alimenta del mismo `form` local** — re-render por teclazo, sin efectos ni store derivado. Nueve filas: Inmueble (alias, :734-739) · Modalidad (:740-745, **enum crudo**: pinta `habitual` en minúscula) · Inicio/Fin (`DateLabel` + `toLocalDate`, :746-757; `toLocalDate` existe para que la fecha no derive un día, `contratoWizardHelpers.ts:25-36`) · Inquilino (:758-765) · Renta mensual (`MoneyValue`, :766-771) · **Renta anual derivada** ×12 (:772-777) · **Fianza derivada** renta×meses (:778-787) · Indexación (:788-791, **enum crudo**: `none` literal, no «Sin indexación» como el select del paso 3 `:481`). Vacíos → «—» con `styles.muted` (`.module.css:141`). **No refleja los pasos 4-5** (coherente: no tienen estado). Pieza a conservar; para reutilizarla habría que extraerla (hoy no es exportable) y formatear los dos enums.

---

## 8 · Encaje con el modelo objetivo

**Nota transversal previa (previsiones de renta):** crear/editar/finalizar un contrato **no genera ni regenera eventos**. `contractService` solo toca `treasuryEvents` en el borrado en cascada (`:243-253,:282-298`). La generación real es `treasurySyncService.generateMonthlyForecasts` §3 (`:289-367`; lee `fechaInicio/fin/estadoContrato/diaPago/rentaMensual/cuentaCobroId/inmuebleId/habitacionId` + plan de gestión), que solo corre dentro de `regenerateForecastsForward` (`treasuryBootstrapService.ts:115`) — cuyos 5 llamadores (gastos, nómina, inversiones, onboarding) **no son de contratos**. La segunda implementación `regenerateRentalsForecast` (`treasuryForecastService.ts:487`) es alcanzable solo desde `regenerateMonthForecast` (:715), con **0 llamadores**. Bugs anexos: los eventos del sync no llevan `contratoId` (`treasurySyncService.ts:330-366`) mientras la cascada de borrado filtra por `ev.contratoId` (`contractService.ts:246,:286`) → el preview/cascade da 0 para rentas reales (workarounds por `sourceId` en `post-open.ts:585-589` y `estadoCobroContratoService.ts:29`); comentario obsoleto «y regenera previsiones» en `contractLifecycleService.ts:5`; `sin_firmar` y `sin_identificar` cuentan como activos y SÍ emiten rentas (`treasurySyncService.ts:112`, `contractService.ts:467`); convención de ids de habitación inconsistente (`hab-N` wizards vs `HN` importador, `contractImportCreationService.ts:102`).

| Pieza | Estado | Evidencia |
|---|---|---|
| Wizard 5 pasos + resumen en vivo | **Se conserva con ajuste** — estructura, validación por pasos, verificación de relectura y resumen funcionan; los pasos 4-5 son stubs y el resumen es JSX inline con 2 enums crudos | `NuevoContratoWizard.tsx:27,100-130,177-180,732-792`; stubs `:520-527,:616-619` |
| Multi-RC vía «accesorio que se alquila junto» | **Se conserva con ajuste (parcial)** — cubre exactamente «parking/trastero con RC propia junto a un piso», 1:1, en `vinculosAccesorio`; NO llega a «Hab 4 + Finca 5» (sin habitación en el vínculo, sin N accesorios), no se escribe en edición/borrador, no se cierra al fin del contrato y fiscal no lo lee | `CampoAccesorioContrato.tsx:12-45`; `vinculoAccesorioService.ts:4-8,77-95`; `types-fiscal.ts:546-557`; huecos `NuevoContratoWizard.tsx:58,170,181`; `irpfCalculationService.ts:643-665` (grep `vinculosAccesorio` → 0) |
| Elección desde catálogo de alquilables (entero/habitaciones) | **Falta** — no existe entidad «unidad alquilable» (grep `unidadAlquilable` → 0); el selector va directo a `properties` y las habitaciones se inventan de `bedrooms`, ignorando `modoExplotacion`/`unidadesArrendables` | `NuevoContratoWizard.tsx:31,98,295-329`; `types-inmuebles.ts:146,155`; grep en wizards → 0 |
| Dos ejes explícitos (régimen × gestión) | **Falta (mezclados)** — no hay campo `regimen` (grep → 0); el eje régimen es `modalidad` (3 valores) y el eje gestión es la presencia de `gestion`/`gestionPadreId`; la delegada hardcodea `modalidad:'habitual'`, así que el eje régimen no existe para gestión | `types-contratos.ts:167,314-327`; `gestionGarantizadaPayload.ts:124-125` |
| Garantizada con bruto diferido (neto vs bruto) | **Se conserva con ajuste (parcial)** — el flujo delegada con renta garantizada, liquidación y comisiones existe y está bien resuelto en tesorería (`planificarGestionMes`); pero **solo se persiste el neto**: el bruto fiscal depende de anexar subcontratos a mano y sin ellos el ingreso desaparece del IRPF | `NuevoContratoGestionWizard.tsx:169-208`; `types-contratos.ts:121-155`; `gestionTesoreria.ts:90-153`; `irpfCalculationService.ts:789-793,893-902`; greps `rentaBruta|brutoFiscal` → 0 |
| Reducción IRPF por régimen+fecha | **Falta en el wizard / parcial en fiscal** — el modelo la soporta (`reduccion` + flags) y `irpfCalculationService` la calcula por fecha (60/50) con override; pero el wizard no captura nada, los flags 70/90 no tienen escritor y hay un segundo motor con campo fantasma fijo al 60 % | `types-contratos.ts:276-290`; `irpfCalculationService.ts:322-367`; único escritor `contractImportCreationService.ts:137-140`; `fiscalSummaryService.ts:585-595` |
| Renta global vs desglosable por unidad | **Falta el desglose** — `rentaMensual` escalar único; por-unidad = N contratos (hermanos por habitación, o hijos `gestionPadreId` en delegada) | `types-contratos.ts:191,219`; greps `rentas?:|desglose` en el tipo → 0; `facturacionGestionService.ts:180-183` |

### Piezas buenas confirmadas para reutilizar (además de la tabla)
- Bifurcación Paso 0 limpia y testeada (`NuevoContrato.tsx:24-25`, `__tests__/NuevoContrato.test.tsx:25-52`) y un solo camino de persistencia (`saveContract`) para los tres escritores UI.
- Verificación de relectura tras el `add` (`NuevoContratoWizard.tsx:177-180`) — atrapa fallos silenciosos de IndexedDB.
- `toLocalDate` para fechas sin deriva de día (`contratoWizardHelpers.ts:25-36`).
- Capa delegada de tesorería (`gestionTesoreria.planificarGestionMes`) con la semántica neto/bruto correcta en cobros.
- Whitelist consciente de `updateContract` en edición (no pisa firma/estado/histórico) (`NuevoContratoWizard.tsx:148-164`).
- `AnexarSubcontratoForm` como patrón de subcontrato mínimo (hereda `diaPago`, `cuentaCobroId: 0` porque cobra la agencia, `anexarSubcontratoPayload.ts:55-79`) — y su reset de habitación al cambiar de piso (`:133-135`), que el wizard principal debería copiar.
